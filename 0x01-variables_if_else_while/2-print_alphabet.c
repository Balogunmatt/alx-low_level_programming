#include <stdio.h>

/**
 * main - entry point
 *
 * Description: using the main function
 * This project is about using putchar function
 * Return: 0
 */

int main(void)
{
	int ch;
	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	return (0);
}
