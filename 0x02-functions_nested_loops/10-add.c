#include "main.h"

/**
 * add - computes the sum of two numbers
 * @c: an integer operand
 * @n: the second operand which is an integer
 *
 * Return: 0 on success
 */

int add(int c, int n)
{
	int i = c + n;

	return (i);
}
