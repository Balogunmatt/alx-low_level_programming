#include <stdio.h>

/**
 * main - entry point
 *
 * Description: this program uses printf instead of puts
 * this prints out with proper grammar, but the outcome is a piece of art,
 * Return: 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
